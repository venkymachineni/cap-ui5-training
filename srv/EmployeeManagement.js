// const cds = require("@sap/cds");

// module.exports = cds.service.impl(async (srv) => {
//   srv.after("READ", "Employee", (results) => {
//     results.forEach((res) => {
//       res.age = res.age + 45;
//     });
//   });
// });